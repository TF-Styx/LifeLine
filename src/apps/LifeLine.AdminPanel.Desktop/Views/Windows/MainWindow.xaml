<Window x:Class="LifeLine.AdminPanel.Desktop.Views.Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LifeLine.AdminPanel.Desktop.Views.Windows"
        xmlns:uc="clr-namespace:Shared.WPF.Views.UserControls;assembly=Shared.WPF" xmlns:windows="clr-namespace:LifeLine.AdminPanel.Desktop.ViewModels.Windows" d:DataContext="{d:DesignInstance Type=windows:MainWindowVM}"
        mc:Ignorable="d"
        Title="MainWindow" 
        d:Height="450" 
        d:Width="800" 
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource App.MainColor}">
    <!--Height="650" Width="800"-->
    <Grid>
        <uc:LoginControl Height="500" Width="400" 
                         Login="{Binding AuthController.Login}" 
                         Password="{Binding AuthController.Password}" 
                         AuthCommand="{Binding AuthCommandAsync}" 
                         Visibility="{Binding AuthVisibility}"
                         d:Visibility="Collapsed"/>
    </Grid>
</Window>
