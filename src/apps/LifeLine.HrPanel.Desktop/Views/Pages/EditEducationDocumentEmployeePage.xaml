<Page x:Class="LifeLine.HrPanel.Desktop.Views.Pages.EditEducationDocumentEmployeePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:LifeLine.HrPanel.Desktop.Views.Pages" 
      xmlns:b="http://schemas.microsoft.com/xaml/behaviors" 
      xmlns:behaviors="clr-namespace:Shared.WPF.Resources.Behaviors;assembly=Shared.WPF" 
      xmlns:cc="clr-namespace:Shared.WPF.Resources.Controls;assembly=Shared.WPF" 
      xmlns:pages="clr-namespace:LifeLine.HrPanel.Desktop.ViewModels.Pages" 
      d:DataContext="{d:DesignInstance Type=pages:EditEducationDocumentEmployeePageVM}"
      mc:Ignorable="d" 
      d:Height="auto" d:Width="auto"
      Title="EditEducationDocumentEmployeePage">

    <b:Interaction.Behaviors>
        <behaviors:AsyncInitializeBehavior/>
    </b:Interaction.Behaviors>

    <Page.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontFamily" Value="Arial"/>
        </Style>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto" Background="{StaticResource App.MainColor}">
            <Border Background="{StaticResource App.SecondColor}" Margin="5" CornerRadius="6" BorderThickness="0">
                <StackPanel Margin="5">
                    <!-- Персональные данные -->
                    <TextBlock Text="Персональные документы" HorizontalAlignment="Center" FontSize="20" Margin="0 0 0 10"/>

                    <!-- Тип документа -->
                    <ComboBox ItemsSource="{Binding EducationLevels}" 
                          SelectedItem="{Binding SelectedEducationLevel}"
                          DisplayMemberPath="Name" 
                          Margin="0 0 0 10"/>

                    <!-- Тип документа -->
                    <ComboBox ItemsSource="{Binding DocumentTypes}" 
                          SelectedItem="{Binding SelectedDocumentType}"
                          DisplayMemberPath="Name" 
                          Margin="0 0 0 10"/>

                    <!-- Данные документа -->
                    <cc:PlaceHolderTextBox PlaceHolder="Номер документа" Text="{Binding EducationDocumentDisplay.DocumentNumber}" Margin="0 0 0 5" Style="{StaticResource TextBox.PlaceHolder}"/>
                    <DatePicker SelectedDate="{Binding EducationDocumentDisplay.IssuedDate}" Margin="0 0 0 5" />
                    <cc:PlaceHolderTextBox PlaceHolder="Наименование организации" Text="{Binding EducationDocumentDisplay.OrganizationName}" Margin="0 0 0 5" Style="{StaticResource TextBox.PlaceHolder}"/>
                    <cc:PlaceHolderTextBox PlaceHolder="Наименование квалификации" Text="{Binding EducationDocumentDisplay.QualificationAwardedName}" Margin="0 0 0 5" Style="{StaticResource TextBox.PlaceHolder}"/>
                    <cc:PlaceHolderTextBox PlaceHolder="Наименование специальности" Text="{Binding EducationDocumentDisplay.SpecialtyName}" Margin="0 0 0 5" Style="{StaticResource TextBox.PlaceHolder}"/>
                    <cc:PlaceHolderTextBox PlaceHolder="Количество часов" Text="{Binding EducationDocumentDisplay.TotalHours}" Margin="0 0 0 5" Style="{StaticResource TextBox.PlaceHolder}"/>
                </StackPanel>
            </Border>
        </ScrollViewer>

        <Button Grid.Row="1" Content="Сохранить" Command="{Binding UpdateEducationDocumentEmployeeCommand}" Height="40" VerticalAlignment="Bottom" Margin="5" Style="{StaticResource App.Button}"/>
    </Grid>

</Page>
