<UserControl x:Class="LifeLine.HrPanel.Desktop.Views.UserControls.WorkPermits"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LifeLine.HrPanel.Desktop.Views.UserControls" 
             xmlns:cc="clr-namespace:Shared.WPF.Resources.Controls;assembly=Shared.WPF" 
             xmlns:features="clr-namespace:LifeLine.HrPanel.Desktop.ViewModels.Features" 
             d:DataContext="{d:DesignInstance Type=features:WorkPermitsVM}"
             xmlns:enum="clr-namespace:LifeLine.HrPanel.Desktop.Enums"
             mc:Ignorable="d" 
             d:Height="auto" 
             d:Width="800">

    <UserControl.Resources>
        <ResourceDictionary>
            <!-- СТИЛЬ ДЛЯ ГРУППИРОВКИ -->
            <Style x:Key="DataGrid.GroupItemStyle" TargetType="GroupItem">
                <Setter Property="Margin" Value="0"/>
                <Setter Property="Padding" Value="0,0,0,5"/>
                <Setter Property="Background" Value="{StaticResource App.MainColor}"/>
                <Setter Property="HorizontalAlignment" Value="Stretch"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="GroupItem">
                            <Grid Margin="{TemplateBinding Margin}" HorizontalAlignment="Stretch">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>

                                <ToggleButton x:Name="ExpanderButton" 
                                              IsChecked="True"
                                              Cursor="Hand"
                                              Background="{TemplateBinding Background}"
                                              Padding="{TemplateBinding Padding}"
                                              BorderThickness="0"
                                              HorizontalContentAlignment="Stretch">
                                    <ToggleButton.Template>
                                        <ControlTemplate TargetType="ToggleButton">
                                            <Border Background="{TemplateBinding Background}"
                                                    CornerRadius="3"
                                                    Margin="{TemplateBinding Padding}" 
                                                    SnapsToDevicePixels="True">
                                                <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                      VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                                            </Border>
                                        </ControlTemplate>
                                    </ToggleButton.Template>

                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>

                                        <!-- Стрелка по умолчанию (вниз) -->
                                        <Path x:Name="ArrowPath" Fill="White" Margin="13,0,0,0" VerticalAlignment="Center"
                                              Data="M 0 0 L 4 4 L 8 0 Z"/>

                                        <WrapPanel Grid.Column="1" Margin="5,0,0,0">
                                            <TextBlock Text="{Binding Name, Converter={StaticResource SaveStatusToStringConverter}}" FontWeight="Bold" FontSize="18" Foreground="#A0D0FF" VerticalAlignment="Center"/>
                                            <TextBlock Text="{Binding ItemCount, StringFormat=' ({0})'}" FontSize="18" Foreground="#88FFFFFF" Margin="8,0,8,0" VerticalAlignment="Center"/>
                                        </WrapPanel>
                                    </Grid>
                                </ToggleButton>

                                <!-- DataGrid автоматически подставляет DataGridRowsPresenter внутрь ItemsPresenter -->
                                <ItemsPresenter x:Name="ItemsPanel" Grid.Row="1" SnapsToDevicePixels="True"/>
                            </Grid>

                            <ControlTemplate.Triggers>
                                <Trigger SourceName="ExpanderButton" Property="IsChecked" Value="False">
                                    <Setter TargetName="ItemsPanel" Property="Visibility" Value="Collapsed"/>
                                    <Setter TargetName="ArrowPath" Property="Data" Value="M 0 4 L 4 0 L 8 4 Z"/>
                                    <!-- Стрелка вверх -->
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>
    
    <Grid Grid.Row="1">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Список сертификатов -->
        <Border Grid.Column="0" Background="{StaticResource App.SecondColor}" CornerRadius="6" Margin="0 0 5 0">
            <Border Margin="5" Background="{StaticResource App.MainColor}" CornerRadius="6" BorderBrush="#386FD6" BorderThickness="2">
                <DataGrid ItemsSource="{Binding WorkPermits.WorkPermitsView}" 
                          SelectedItem="{Binding WorkPermits.SelectedLocalWorkPermit}"
                          AutoGenerateColumns="False" 
                          CanUserAddRows="False"
                          HorizontalContentAlignment="Stretch" 
                          HeadersVisibility="Column"
                          Margin="2"
                          Background="{StaticResource App.MainColor}"
                          d:ItemsSource="{d:SampleData ItemCount=10}">

                    <DataGrid.GroupStyle>
                        <GroupStyle>
                            <GroupStyle.ContainerStyle>
                                <StaticResource ResourceKey="DataGrid.GroupItemStyle"/>
                            </GroupStyle.ContainerStyle>
                        </GroupStyle>
                    </DataGrid.GroupStyle>
                    
                    <DataGrid.Columns>
                        <!-- Тип документа -->
                        <DataGridTemplateColumn Header="Тип документа" Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding PermitType.Name}" 
                                               TextTrimming="CharacterEllipsis" TextWrapping="NoWrap"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- Статус -->
                        <DataGridTemplateColumn Header="Статус" Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding AdmissionStatus.Name}" 
                                               TextTrimming="CharacterEllipsis" TextWrapping="NoWrap"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- Наименование -->
                        <DataGridTemplateColumn Header="Наименование" Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding WorkPermitName}" 
                                               TextTrimming="CharacterEllipsis" TextWrapping="NoWrap"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!--Информация-->
                        <DataGridTemplateColumn Width="35">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Width="25" Command="{Binding DeleteImageCommand}" Style="{StaticResource Button.Icon}">
                                        <Control Template="{StaticResource Magnifier}"/>
                                        <Button.ToolTip>
                                            <StackPanel>
                                                <TextBlock Text="{Binding PermitType.Name, StringFormat=Типа документа: {0}}"/>
                                                <TextBlock Text="{Binding AdmissionStatus.Name, StringFormat=Статус документа: {0}}"/>
                                                <TextBlock Text="{Binding WorkPermitName, StringFormat=Наименование: {0}}"/>
                                                <TextBlock Text="{Binding DocumentSeries, StringFormat=Серия: {0}}"/>
                                                <TextBlock Text="{Binding WorkPermitNumber, StringFormat=Номер разрешения на работу: {0}}"/>
                                                <TextBlock Text="{Binding ProtocolNumber, StringFormat=Номер протокола: {0}}"/>
                                                <TextBlock Text="{Binding SpecialtyName, StringFormat=Наименование специальности: {0}}"/>
                                                <TextBlock Text="{Binding IssuingAuthority, StringFormat=Орган выдающий документы: {0}}"/>
                                                <TextBlock Text="{Binding IssueDate, StringFormat=Дата выдачи: {0}}"/>
                                                <TextBlock Text="{Binding ExpiryDate, StringFormat=Дата истечения срока действия: {0}}"/>
                                                <TextBlock Text="{Binding FilePath, StringFormat=Файл: {0}}"/>
                                            </StackPanel>
                                        </Button.ToolTip>
                                    </Button>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!--Удалить-->
                        <DataGridTemplateColumn Width="35">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Width="25" Foreground="#F06870" 
                                            Command="{Binding DataContext.DeleteWorkPermitCommand, 
                                            RelativeSource={RelativeSource AncestorType=DataGrid}}" 
                                            CommandParameter="{Binding }" 
                                            Style="{StaticResource Button.Icon}">
                                        <Control Template="{StaticResource Trash}"/>
                                    </Button>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </Border>


        <Border Grid.Column="1" Background="{StaticResource App.SecondColor}" CornerRadius="6">
            <Grid Margin="5">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="50"/>
                    <RowDefinition Height="50"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0">
                    <!-- Тип документа / Статус -->
                    <ComboBox Grid.Column="0" 
                              ItemsSource="{Binding PermitTypes}" 
                              SelectedItem="{Binding WorkPermits.Display.PermitType}"
                              DisplayMemberPath="Name" 
                              Margin="0 0 0 5"/>

                    <ComboBox Grid.Column="1" 
                              ItemsSource="{Binding AdmissionStatuses}" 
                              SelectedItem="{Binding WorkPermits.Display.AdmissionStatus}"
                              DisplayMemberPath="Name" 
                              Margin="0 5 0 10"/>

                    <!-- Наименование документа / Наименование специальности -->
                    <cc:PlaceHolderTextBox Grid.Column="0" PlaceHolder="Наименование документа*" Margin="0 0 0 5"
                                           Text="{Binding WorkPermits.Display.WorkPermitName}" Style="{StaticResource TextBox.PlaceHolder}"/>

                    <cc:PlaceHolderTextBox Grid.Column="1" PlaceHolder="Наименование специальности*" Margin="0 0 0 5"
                                           Text="{Binding WorkPermits.Display.SpecialtyName}" Style="{StaticResource TextBox.PlaceHolder}"/>

                    <!-- Серия / Номер -->
                    <cc:PlaceHolderTextBox Grid.Column="0" PlaceHolder="Серия" Margin="0 0 0 5"
                                           Text="{Binding WorkPermits.Display.DocumentSeries}" Style="{StaticResource TextBox.PlaceHolder}"/>

                    <cc:PlaceHolderTextBox Grid.Column="1" PlaceHolder="Номер*" Margin="0 0 0 5"
                                           Text="{Binding WorkPermits.Display.WorkPermitNumber}" Style="{StaticResource TextBox.PlaceHolder}"/>

                    <!-- Номер протокола / Орган выдающий документы -->
                    <cc:PlaceHolderTextBox Grid.Column="0" PlaceHolder="Номер протокола" Margin="0 0 0 5"
                                           Text="{Binding WorkPermits.Display.ProtocolNumber}" Style="{StaticResource TextBox.PlaceHolder}"/>

                    <cc:PlaceHolderTextBox Grid.Column="1" PlaceHolder="Орган выдающий документы*" Margin="0 0 0 10"
                                           Text="{Binding WorkPermits.Display.IssuingAuthority}" Style="{StaticResource TextBox.PlaceHolder}"/>

                    <!-- Дата выдачи / Дата окончания -->
                    <DatePicker Grid.Column="0" SelectedDate="{Binding WorkPermits.Display.IssueDate}" Margin="0 0 0 10"/>

                    <DatePicker Grid.Column="1" SelectedDate="{Binding WorkPermits.Display.ExpiryDate}" Margin="0 0 0 10"/>

                    <!-- Файл -->
                    <StackPanel Grid.Row="1" Margin="0 10 0 0">
                        <Button Content="Выбрать скан" 
                                    Width="170" Height="40" 
                                    HorizontalAlignment="Right" Margin="0 0 0 5"
                                    Command="{Binding WorkPermits.SelectMultipleCommand}" 
                                    Style="{StaticResource App.Button}"/>

                        <ListView ItemsSource="{Binding WorkPermits.PendingFilePaths}"
                                      d:ItemsSource="{d:SampleData ItemCount=10}"
                                      Background="#1E1E1E"
                                      Style="{StaticResource Custom.ListView}"
                                      ItemContainerStyle="{StaticResource Custom.ListViewItem}">

                            <ListView.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel Orientation="Horizontal" IsItemsHost="True" Margin="0"/>
                                </ItemsPanelTemplate>
                            </ListView.ItemsPanel>

                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="{StaticResource App.SecondColor}" CornerRadius="6">
                                        <Border BorderBrush="#386FD6" 
                                                    Background="{StaticResource App.MainColor}" 
                                                    BorderThickness="2" 
                                                    CornerRadius="10" 
                                                    HorizontalAlignment="Left" 
                                                    VerticalAlignment="Top" 
                                                    Margin="5">
                                            <StackPanel>
                                                <Border BorderThickness="0 0 0 2" BorderBrush="#386FD6">
                                                    <StackPanel>
                                                        <Grid Width="150" Height="150" Margin="5">
                                                            <Image Source="{Binding Thumbnail}" 
                                                                       d:Source="C:\Users\texno\Documents\Lightshot\Screenshot_4.png"
                                                                       Visibility="{Binding Thumbnail, Converter={StaticResource NullToVisibilityConverter}}"/>

                                                            <TextBlock Text="{Binding FileExtension}"
                                                                           FontSize="24"
                                                                           FontWeight="Bold"
                                                                           Foreground="#AAAAAA"
                                                                           HorizontalAlignment="Center"
                                                                           VerticalAlignment="Center"
                                                                           Visibility="{Binding Thumbnail, Converter={StaticResource NotNullToVisibilityConverter}}"/>
                                                        </Grid>

                                                        <TextBlock Text="{Binding Index, StringFormat='#{0}'}"
                                                                       HorizontalAlignment="Center" 
                                                                       FontSize="18" 
                                                                       Margin="0 0 0 2"
                                                                       Foreground="White"
                                                                       FontWeight="Bold"/>
                                                        <TextBlock Text="{Binding FileName}"
                                                                       HorizontalAlignment="Center" 
                                                                       FontSize="12" 
                                                                       Margin="0 0 0 2"
                                                                       Foreground="#AAAAAA"
                                                                       TextTrimming="CharacterEllipsis"
                                                                       MaxWidth="140"/>
                                                        <TextBlock Text="{Binding FileSizeFormatted}"
                                                                       HorizontalAlignment="Center" 
                                                                       FontSize="10" 
                                                                       Margin="0 0 0 5"
                                                                       Foreground="#666666"/>
                                                    </StackPanel>
                                                </Border>

                                                <!-- Кнопки управления -->
                                                <WrapPanel HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0 2 0 2">
                                                    <Button Width="25" 
                                                                Background="Transparent" 
                                                                BorderThickness="0"
                                                                Margin="0 0 10 0"
                                                                Command="{Binding DataContext.WorkPermits.PreviewCommand, 
                                                                                  RelativeSource={RelativeSource AncestorType=ListView}}"
                                                                CommandParameter="{Binding }"
                                                                Style="{StaticResource Button.Icon}">
                                                        <Control Template="{StaticResource Magnifier}"/>
                                                    </Button>
                                                    <Button Width="25" 
                                                                Background="Transparent" 
                                                                BorderThickness="0"
                                                                Foreground="#F06870"
                                                                Margin="10 0 0 0"
                                                                Command="{Binding DataContext.WorkPermits.RemovePendingFileCommand, 
                                                                                  RelativeSource={RelativeSource AncestorType=ListView}}"
                                                                CommandParameter="{Binding }"
                                                                Style="{StaticResource Button.Icon}">
                                                        <Control Template="{StaticResource Trash}"/>
                                                        <Button.ToolTip>
                                                            <TextBlock Text="Удалить"/>
                                                        </Button.ToolTip>
                                                    </Button>
                                                </WrapPanel>
                                            </StackPanel>
                                        </Border>
                                    </Border>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </StackPanel>
                </StackPanel>

                <Button Grid.Row="1" Content="Добавить локально" Height="40"
                        HorizontalAlignment="Stretch" VerticalAlignment="Center"
                        Command="{Binding WorkPermits.AddWorkPermitCommandAsync}"
                        Style="{StaticResource App.Button}" Margin="0 10 0 0"/>

                <Button Grid.Row="2" Content="Продолжить" Height="40"
                        HorizontalAlignment="Stretch" VerticalAlignment="Center"
                        Command="{Binding ExecuteStepCommand}" 
                        CommandParameter="{x:Static enum:EmployeeCreationSteps.Specialties}"
                        Visibility="{Binding Action, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource TypeActionConverter}, ConverterParameter={x:Static enum:TypeAction.CreateStep}, UpdateSourceTrigger=PropertyChanged}"
                        Style="{StaticResource App.Button}" Margin="0 10 0 0"/>

                <Button Grid.Row="2" Content="Сохранить" Height="40"
                        HorizontalAlignment="Stretch" VerticalAlignment="Center"
                        Command="{Binding CreateWorkPermitCommand}"
                        Visibility="{Binding Action, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource TypeActionConverter}, ConverterParameter={x:Static enum:TypeAction.Create}, UpdateSourceTrigger=PropertyChanged}"
                        Style="{StaticResource App.Button}" Margin="0 10 0 0"/>

                <Button Grid.Row="2" Content="Обновить" Height="40"
                        HorizontalAlignment="Stretch" VerticalAlignment="Center"
                        Command="{Binding UpdateWorkPermitCommand}"
                        Visibility="{Binding Action, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource TypeActionConverter}, ConverterParameter={x:Static enum:TypeAction.Update}, UpdateSourceTrigger=PropertyChanged}"
                        Style="{StaticResource App.Button}" Margin="0 10 0 0"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
